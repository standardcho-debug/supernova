pytest>=8.0
# anthropic>=0.40.0  # optional: only needed to use AnthropicLLMClient
# playwright>=1.45.0  # optional: only needed to use PlaywrightCardRenderer
#   after installing, also run: playwright install chromium
